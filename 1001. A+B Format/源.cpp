#include <stdio.h>

int main() {
	int a = 0, b = 0;
	int sum = 0;
	scanf("", &a, &b);
	sum = a + b;

	if (sum < 0) {
		sum = -sum;
		printf("-");
	}

	int num = 0;

	while (sum != 0) {
		num = sum / 1000
	}
}